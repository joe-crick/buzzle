# Configure pnpm for React Native/Metro compatibility
# Use hoisted node linker to approximate flat node_modules structure
node-linker=hoisted

# Ensure key React Native/Metro packages are publicly hoisted for resolvers
public-hoist-pattern[]=@react-native/*
public-hoist-pattern[]=react-native
public-hoist-pattern[]=react-native-*
public-hoist-pattern[]=metro
public-hoist-pattern[]=metro-*
public-hoist-pattern[]=jest
public-hoist-pattern[]=eslint
